defaults:
  - setting: default
hydra:
  run:
    dir: ./result/${setting.run_file}/n_users=${setting.n_users},n_action=${setting.n_action}
  sweep:
    dir: ./result/${setting.run_file}/n_users=${setting.n_users},n_action=${setting.n_action}
    subdir: ./